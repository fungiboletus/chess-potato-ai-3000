[project]
name = "chess-potato-ai-3000"
version = "0.1.0"
description = "A chess AI that tries to play like a beginner."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["chess>=1.11.2", "requests>=2.32.5"]

[dependency-groups]
dev = ["pytest>=8.4.2", "ruff>=0.14.4"]
